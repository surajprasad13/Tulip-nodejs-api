Feature: Survey

  # Scenario: generate progress bar index
  #   Given the plan "fatigue" is selected
  #   When the I generate metadata for the survey
  #   Then the survey contains metadata for progress bar

  # Scenario: generate progress bar index
  #   Given the plan "dev_free_long_covid" is selected
  #   When the I generate metadata for the survey
  #   Then the survey contains metadata for progress bar

  # Scenario: generate progress bar index
  #   Given the plan "sleep" is selected
  #   When the I generate metadata for the survey
  #   Then the survey contains metadata for progress bar
